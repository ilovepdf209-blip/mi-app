import React from 'react';

const SplashCursor: React.FC = () => {
  return (
    <div className="fixed top-0 left-0 z-50 pointer-events-none w-full h-full">
      {/* Componente simplificado - sin efectos de WebGL */}
    </div>
  );
};

export default SplashCursor;
