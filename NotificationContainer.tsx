import React from 'react';

const NotificationContainer: React.FC = () => {
  return (
    <div className="fixed top-4 right-4 z-50 space-y-4">
      {/* Notifications will be added here when needed */}
    </div>
  );
};

export default NotificationContainer; 